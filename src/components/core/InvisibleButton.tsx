import styled from "styled-components";

export default styled.button`
    border: none;
    background: none;
    margin: 0;
    padding: 0;

    :hover {
        background-color: rgba(0, 0, 0, 0.5);
    }
`;
